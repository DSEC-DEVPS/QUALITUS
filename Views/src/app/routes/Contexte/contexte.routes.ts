import { Routes } from '@angular/router';
import { ListeContexteComponent } from './liste-contexte/liste-contexte.component';

export const routes: Routes = [{ path: 'Liste', component: ListeContexteComponent }];
